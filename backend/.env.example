# Amazon Cognito (phone OTP) — public app client required (no client secret in browser).
# If phone OTP fails with "invalid client", create a new public app client in Cognito
# (App integration → App clients → Create → Public) and replace COGNITO_CLIENT_ID.
COGNITO_REGION=ap-southeast-2
COGNITO_USER_POOL_ID=ap-southeast-2_YPMktk5Ex
COGNITO_CLIENT_ID=2vqtsu3lavdg8g5bp5ugt7c9gm
